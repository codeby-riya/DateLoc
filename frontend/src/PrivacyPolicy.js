import React from 'react';

const PrivacyPolicy = () => (
  <div className="container mt-5">
    <h1>Privacy Policy</h1>
    <p>This app does not store any personal data. OAuth tokens are used only for scheduling emails and are never shared.</p>
  </div>
);

export default PrivacyPolicy;
